## 1. 看门狗配置与队列恢复

- [x] 1.1 在 `backend/config.py` 增加经过正数校验的生成执行时限配置及安全默认值，并通过单元测试验证合法覆盖值、零值和非数值均符合预期。
- [x] 1.2 在串行生成 worker 中为每条运行中任务施加该时限；超时时取消任务、写入固定的失败原因并清理运行态，验证超时任务不再阻塞后续任务。
- [x] 1.3 保持已存在的取消语义：超时后跳过已取消的排队项并继续下一条任务，验证运行中任务、已取消排队任务和后续正常任务的状态及执行顺序。

## 2. 回归测试与验证

- [x] 2.1 在 `backend/tests/test_task_queue_cancellation.py`（或同职责测试文件）补充可控异步测试：永不结束的首任务超时后被取消、第二任务在限定时间内执行、队列运行态被清空；运行该文件验证通过。
- [x] 2.2 补充正常任务在默认或覆盖时限内完成且未被误取消的测试；运行定向队列测试与模型运行时韧性测试验证通过。
- [x] 2.3 运行 OpenSpec 严格校验、受影响 Python 测试集和项目静态检查；记录结果并仅在全部通过后勾选任务。
