## 1. 声音档案兼容性

- [x] 1.1 提取档案与引擎的前端兼容性/恢复建议规则，供模型下拉、档案切换和提交前校验共用；通过新增的前端单元测试验证 Vivian 切换为 CustomVoice、克隆声音拒绝 CustomVoice 且声音不变。
- [x] 1.2 在生成表单提交前使用当前档案执行兼容性校验，并将预设/克隆声音冲突映射为中文可操作提示；通过前端测试确认陈旧表单状态不会调用生成 API。
- [x] 1.3 保持后端兼容性校验在生成记录创建前执行，并补充路由级测试验证预设声音与不匹配引擎返回 400 且不新增生成记录。

## 2. 捕获听写失败诊断

- [x] 2.1 实现受控的捕获错误分类与本地 JSONL 诊断写入，记录诊断编号、阶段、非敏感元数据和安全摘要；通过单元测试验证诊断条目不包含音频、转写正文、绝对路径或堆栈。
- [x] 2.2 将空、损坏和无法解码的录音稳定转换为 4xx，并确保失败清除暂存音频；通过服务测试验证损坏 WebM 不返回 500 且捕获目录无孤立文件。
- [x] 2.3 为模型加载、转写和未预期异常返回带诊断编号的安全 500；通过模拟 Whisper 异常的测试验证阶段记录、响应编号和既有捕获数据不受影响。
- [x] 2.4 更新客户端错误格式化和捕获错误提示，使其显示中文说明及可复制诊断编号；通过前端单元测试验证未知服务端 detail 不泄露、受控诊断编号可呈现。

## 3. 验证与桌面应用交付

- [x] 3.1 运行受影响的后端 pytest、前端 bun test 与 TypeScript 检查，并执行 `openspec validate fix-capture-and-preset-generation --strict`，确认全部通过。
- [x] 3.2 使用本地构建流程生成 sidecar 与桌面应用，替换前备份现有应用并验证健康接口；验证安装版界面将预设 Vivian 切换为 CustomVoice、不匹配请求返回 400 且不创建任务，并以有效录音完成一次真实听写后清理临时记录；损坏录音 4xx 与内部错误诊断编号由回归测试覆盖。
