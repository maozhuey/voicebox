## 1. 队列生命周期恢复

- [x] 1.1 为生成队列暴露本次进程内的排队、运行和工作协程健康状态，并实现工作协程异常结束后的受控恢复；通过队列单元测试验证等待任务会继续执行。
- [x] 1.2 实现统一的活动生成记录收口逻辑，使没有当前执行者的 `loading_model`/`generating` 记录变为失败且保留历史内容；通过数据库测试验证状态和错误原因。
- [x] 1.3 在服务启动和运行期监督中调用恢复逻辑，并保证仍排队或运行的任务不被改写；通过生命周期测试验证正常 CosyVoice 风格的长任务状态不受影响。

## 2. 验证与桌面交付

- [x] 2.1 补充队列工作协程停止、任务协程失配、启动遗留记录和正常任务保护的回归测试，并运行相关 pytest。
- [x] 2.2 执行后端受影响测试集与 `openspec validate recover-orphaned-generations-on-startup --strict`，确认规范和实现均通过。
- [x] 2.3 生成本地桌面安装包，备份现有应用后替换并验证健康接口；手工确认重启后不再出现无执行者的“正在生成”记录，且可以创建新任务。
