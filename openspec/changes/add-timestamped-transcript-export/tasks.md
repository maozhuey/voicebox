## 1. 先建立失败测试与数据契约

- [x] 1.1 为结构化转录结果和时间戳文本格式化编写后端单元测试，覆盖正常分段、空文本、非法/倒序边界、毫秒舍入、小时进位和无有效分段失败，并运行该测试确认实现前失败。
- [x] 1.2 为 MLX 与 PyTorch STT 适配编写模型返回桩测试，验证两条后端都返回相同的原始全文及升序毫秒分段，且不会用全时长虚拟分段兜底，并运行测试确认实现前失败。
- [x] 1.3 为 `/transcribe` 编写接口测试，验证保留 `text`、`duration` 且新增 `segments`、`timestamped_text`，同时覆盖缺少有效时间戳时不返回成功结果，并运行测试确认实现前失败。
- [x] 1.4 为捕获数据编写测试，覆盖新建持久化、重启后读取、精修不改变时间戳、重新转录原子替换、重新转录失败保留旧数据、旧行空字段兼容和损坏 JSON 降级，并运行测试确认实现前失败。
- [x] 1.5 为客户端纯导出函数编写 Bun 单元测试，覆盖固定行格式、原始文本来源、分段顺序、空分段与小时级时间码，并运行测试确认实现前失败。

## 2. 实现 Whisper 原始时间戳输出

- [x] 2.1 增加统一的转录结果、分段模型、验证与格式化函数，并通过任务 1.1 的全部测试。
- [x] 2.2 更新 MLX STT 后端以保留模型原始分段，更新 PyTorch STT 后端以解析 Whisper 分段时间戳，并通过任务 1.2 的全部测试。
- [x] 2.3 更新所有 STT 调用点和 `/transcribe` 响应模型，以兼容方式返回结构化分段与格式化时间戳文稿，并通过任务 1.3 的全部测试。
- [x] 2.4 同步 `/transcribe` 的 README、开发者指南、静态 OpenAPI 和生成的桌面端 API 客户端；新增 Large/Turbo 响应契约回归测试，验证两者保留旧字段且均公开时间戳字段，并验证示例使用 `file` 与 `turbo`。

## 3. 持久化捕获时间戳并保护原始数据

- [x] 3.1 给捕获 ORM 增加可空 JSON 文本列并增加幂等 SQLite 列迁移，使用新数据库和缺少该列的旧数据库各执行两次迁移，验证结构正确且无重复迁移错误。
- [x] 3.2 更新捕获创建、读取、列表、精修和重新转录服务：同一提交保存/替换原文与分段、派生格式化文稿、损坏旧数据安全降级，并通过任务 1.4 的全部测试。
- [x] 3.3 更新捕获 Pydantic 响应与前端 TypeScript 类型，运行后端响应序列化测试和前端类型检查验证字段可选性兼容旧记录。

## 4. 增加“带时间戳文稿”导出

- [x] 4.1 实现无 UI 依赖的时间戳 TXT 构建函数，输出 `[HH:MM:SS.mmm --> HH:MM:SS.mmm] 原始文本`，并通过任务 1.5 的全部测试。
- [x] 4.2 在捕获详情“导出”菜单增加“带时间戳文稿”项，保存为 `capture_<短ID>_timestamped.txt`；验证有数据时写入 UTF-8 文件、取消时不写入、失败时显示错误。
- [x] 4.3 增加所有现有语言的菜单和提示文案；验证无时间戳的旧捕获点击后不打开保存框，并提示通过重新转录生成。
- [x] 4.4 在“精修”和“原始”两个查看状态分别导出同一捕获，验证两份文件都逐行等于 Whisper 原始分段且不包含精修替换文本。

## 5. 完整验证与本地验收

- [x] 5.1 运行本变更新增的后端和 Bun 测试，确认所有 OpenSpec 场景都有自动测试或明确的桌面端验收记录且全部通过。
- [ ] 5.2 运行 Voicebox 后端完整 pytest、Ruff 以及前端 TypeScript/biome 检查，确认没有引入回归。
  - 已执行：TypeScript 与变更文件定向检查通过；完整 pytest/Ruff/biome 被仓库既有错误阻断，详情见 `verification.md`。
- [x] 5.3 本地启动 Voicebox，分别用短音频和超过 30 秒的音频完成“导入→转录→精修→导出→重新转录”流程，核对音频播放位置、API 分段、数据库分段和导出文稿时间一致。
- [x] 5.4 对捕获详情导出菜单进行截图验收，确认新增入口的位置、中文文案、缺失时间戳提示和现有 WAV/TXT/Markdown 项均符合设计。
