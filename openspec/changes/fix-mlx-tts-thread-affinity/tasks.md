## 1. MLX TTS 线程亲和性修复

- [x] 1.1 为 `MLXTTSBackend` 添加专用单线程执行器和统一异步提交入口，并验证模型加载与生成不再使用默认线程池。
- [x] 1.2 将 Qwen TTS 的阻塞加载和推理改为通过同一专用执行器运行，并验证既有异步接口与生成队列调用方式保持不变。

## 2. 回归测试

- [x] 2.1 编写不加载真实模型的单元测试，验证加载与生成操作提交到同一个 MLX 执行器；运行该测试确认通过。
- [x] 2.2 运行现有 Qwen/MLX 相关测试，确认新线程调度不破坏模型加载、分块生成和失控输出重试。

## 3. 本机桌面验证

- [x] 3.1 构建修复后的冻结侧车，并在隔离数据目录中完成一次 Qwen 真实生成验证。
- [x] 3.2 重新打包、签名并以保留备份的方式替换本机 Voicebox 应用；启动后通过 `http://127.0.0.1:17493/health` 与一次 Qwen 生成验证。
