## 1. 设备与缓存加载策略

- [x] 1.1 在共享后端工具中实现可测试的 GPU 优先设备选择与单次 CPU 回退契约，并验证 Apple Silicon 的 MPS、无 GPU 的 CPU 与既有 CUDA/XPU/DirectML 优先级单元测试通过。
- [ ] 1.2 迁移 Qwen CustomVoice、CosyVoice、Chatterbox、Chatterbox Turbo、TADA、Kokoro 及 PyTorch 回退路径以使用 GPU 优先策略，并以模拟 GPU 加载/首次推理失败验证每个后端仅回退 CPU 一次。
- [ ] 1.3 为完整 HuggingFace 缓存实现本地优先加载参数或快照解析，并验证网络不可达时完整缓存不产生远端请求、缓存不完整时仍进入可观测下载流程。

## 2. 加载时限与任务终态

- [x] 2.1 在后端配置中增加带安全默认值的模型加载超时环境配置，并验证缺省值、合法覆盖值与非法值的解析行为。
- [x] 2.2 在统一生成编排路径为模型加载应用时限并转换为稳定领域错误，验证超时任务写入 `failed` 和用户可理解的错误文本。
- [ ] 2.3 处理超时后的后台加载隔离和队列清理，验证超时或加载异常后的后续排队任务可以继续执行且不会复用不确定模型实例。
- [ ] 2.4 保持 SSE、历史记录与失败重试兼容，验证客户端可收到终态、历史 API 返回错误、失败任务满足既有 retry 前置条件。

## 3. 回归验证

- [ ] 3.1 新增覆盖 GPU 选择、CPU 回退、本地缓存、加载超时、失败终态和队列恢复的后端测试，并运行这些测试全部通过。
- [ ] 3.2 运行既有生成、模型下载、取消与相关 API 测试，验证没有回归。
- [ ] 3.3 使用本地 Docker 环境启动服务，在完整缓存且远端不可达与加载超时两种条件下验证 API 状态由 `loading_model` 在配置时限内转为 `completed` 或 `failed`；记录实际响应作为验收证据。
