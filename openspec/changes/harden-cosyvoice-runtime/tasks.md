## 1. 资源隔离与持久化诊断

- [x] 1.1 添加仅针对 TTS 后端的资源协调入口和活动执行租约：在 CosyVoice 加载前等待其他 TTS 任务完成，再卸载其空闲模型、回收垃圾并清理 MLX Metal 缓存；使用模拟后端测试确认当前 CosyVoice 不被卸载、Qwen 被先卸载，且运行中的 Qwen 不会被卸载或失败。
- [x] 1.2 为生成记录增加兼容的 CosyVoice 运行阶段与阶段耗时诊断字段，并完成已有 SQLite 数据库的幂等迁移；使用迁移与历史序列化测试确认旧记录为空、新记录可保存且不包含文本/路径。

## 2. 可恢复的 CosyVoice 推理

- [x] 2.1 实现专用 CosyVoice 工作进程及父子消息协议，覆盖模型准备、前处理、LLM 解码、Flow、声码器、结果和安全错误；使用无真实模型的工作器测试确认阶段事件顺序。
- [x] 2.2 为每个 CosyVoice 分段配置正数默认执行时限；超时时终止工作进程、写入带阶段的可重试失败并重置工作器；使用阻塞模拟测试确认超时后进程退出且任务成为失败。
- [x] 2.3 让 CosyVoice 仅按自然语义边界分段并保持自然朗读停顿，不按固定字符数截断；使用中文长句、分号、换行与无标点文本测试确认分段顺序、停顿计划和无边界原文保留。

## 3. 队列与 API 集成

- [x] 3.1 接入生成服务和状态 API/SSE：生成中返回当前 CosyVoice 阶段，完成或失败后保留安全诊断；使用 API/服务测试确认非 CosyVoice 响应保持兼容。
- [x] 3.2 修复 CosyVoice 超时后的串行队列恢复；使用“阻塞 CosyVoice 后排队 Qwen”测试确认后续任务在失败终态后启动。

## 4. 验证与本机桌面交付

- [x] 4.1 运行新增测试及 CosyVoice、自然朗读、任务队列、模型运行时相关回归测试，确认全部通过；运行格式和静态检查并记录既有问题。
- [x] 4.2 在隔离数据目录中先执行 Qwen、再执行 CosyVoice RL 真实生成，确认 Qwen 被释放、CosyVoice 完成并产生阶段诊断；模拟或缩短时限验证超时后队列可恢复。
- [x] 4.3 构建、签名并以保留备份的方式替换本机 Voicebox 应用；通过健康接口、真实 CosyVoice 生成和历史诊断字段验收，失败时恢复备份。

## 5. 连续分段工作进程协议修复

- [x] 5.1 将 worker 就绪状态从单次分段局部变量提升为工作进程生命周期状态；已就绪的同型号 worker 在后续分段中直接提交请求，终止、崩溃或型号切换时清除就绪状态。
- [x] 5.2 分离 worker 首次启动等待与单段推理计时；使用模型加载时限等待首个 `ready`，并在每个请求实际提交后重新起算分段时限。
- [x] 5.3 新增同一 worker 连续处理两个分段的协议回归测试，验证只消费一次 `ready`、两个请求均返回音频、超时后重建 worker 会重新等待 `ready`。
- [x] 5.4 运行 CosyVoice worker、自然语义分段、生成服务与队列回归测试，执行严格 OpenSpec 校验，并在本地开发服务中完成一次真实双段 CosyVoice 生成。
