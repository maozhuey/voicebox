## 1. 终态与诊断基础设施

- [x] 1.1 定义生成运行结果、稳定错误代码和终态发布接口，使运行器只能在明确报告已发布的 `completed` 或 `failed` 终态后释放所有权；通过新增单元测试验证完成、模型异常、取消和超时的结果类别互不混淆。
- [x] 1.2 实现脱敏生成诊断 JSONL 写入器及诊断编号关联；通过测试验证诊断包含引擎、型号、分段进度、退出类别和生命周期类别，且不包含用户正文、参考音频路径、绝对路径、原始异常消息或堆栈。
- [x] 1.3 实现原子服务生命周期标记，记录运行中与正常关闭；通过临时数据目录测试验证上次仍为运行中被归类为异常退出、正常关闭不被误判、损坏标记不会阻止服务启动。

## 2. MLX/Metal 运行时串行化

- [x] 2.1 实现进程级单线程 MLX 运行时协调器，并通过并发单元测试验证同一时刻最多一个 MLX 原生回调执行且所有回调使用同一持久工作线程。
- [x] 2.2 将 MLX TTS 的加载、推理、缓存清理和卸载迁移到共享协调器；通过现有 MLX TTS 测试及新增线程所有权测试验证模型生命周期和分段生成保持正确。
- [ ] 2.3 将 MLX LLM 与 MLX STT 的加载、推理和卸载迁移到共享协调器；通过并发的 TTS 与 LLM/转录替身测试验证操作按提交顺序串行，且不会在后台线程池并行调用 MLX。

## 3. 后台队列提前结束修复

- [x] 3.1 修改生成运行器，使最终状态写入成功后才报告终态，并将状态提交失败转换为可恢复的明确运行结果；通过生成服务测试验证成功生成保留音频路径/时长且只发布一次完成终态。
- [x] 3.2 修改生成队列，在运行器返回后使用独立数据库会话核验终态；通过三段生成替身测试复现“第 1/3 段后返回”，验证记录变为带诊断编号的终态未发布失败、进度保持 1/3，并且不再写入泛化中断错误。
- [ ] 3.3 为工作协程异常和终态恢复写入失败增加受控收敛；通过 SQLite 锁/首次提交失败替身测试验证当前任务先获得可识别失败，随后排队任务仍被执行，队列不会提前释放或永久阻塞。
- [x] 3.4 保留取消、超时、正常完成和服务关闭的已有原因；扩展任务队列取消测试，验证队列兜底不会覆盖这些终态，且所有分支均会清理运行中任务跟踪。
- [x] 3.5 将启动孤儿恢复接入生命周期标记；通过异常服务退出与正常关闭两组数据库测试验证遗留活动任务分别获得不同、可重试且带诊断编号的失败原因，不删除正文或已有音频。

## 4. 客户端错误呈现

- [x] 4.1 扩展客户端错误映射，识别终态未发布和本地服务异常退出的稳定错误代码；通过 Vitest 测试验证文案含分段进度、可重试建议和诊断编号，且不退化为“生成过程中发生错误”或“模型加载失败”。
- [ ] 4.2 在生成历史与实时状态消费处保留后端诊断编号；通过组件/状态测试验证失败卡片与通知展示同一安全错误，不暴露原始异常或朗读正文。

## 5. 整体验证

- [x] 5.1 运行相关后端测试（生成服务、任务队列、服务生命周期、MLX 运行时）并确认全部通过；至少覆盖三段任务、第 1/3 段无终态返回、取消、超时、服务异常退出和后续任务继续执行。
- [x] 5.2 运行前端测试、类型检查和生产构建，确认错误映射没有破坏既有生成、捕获或精修流程。
- [ ] 5.3 启动本地开发服务并进行手工验收：执行一次三段 Qwen 合成，同时触发一次捕获精修；确认 MLX 工作不并发、生成记录最终有明确状态，异常模拟后显示可重试诊断而非泛化中断。
- [x] 5.4 执行 `openspec validate fix-generation-terminal-status --strict`，确认提案、设计、规格和任务清单均通过严格校验。
