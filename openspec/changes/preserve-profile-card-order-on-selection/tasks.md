## 1. 列表顺序实现

- [x] 1.1 将声音档案列表改为直接按查询结果顺序渲染，同时保留逐卡兼容性禁用态和不兼容提示；验证：在固定的混合引擎档案数据中，渲染顺序与接口数组顺序一致。

## 2. 回归测试

- [x] 2.1 为“点击非首张卡片”的场景新增组件测试；验证：点击前后读取三张或更多卡片的可访问名称/稳定标识，顺序相同，且被点击卡片处于选中态。
- [x] 2.2 为“选择导致生成引擎变化”和“再次点击取消选择”的场景新增组件测试；验证：两次状态更新后的卡片顺序均与初始顺序相同，不支持卡片仍为禁用态，取消选择后不再显示选中态。
- [x] 2.3 为“档案集合刷新”场景新增组件测试；验证：模拟查询结果替换后，卡片按新数组顺序渲染，而不是保留已删除或旧数据的位置。

## 3. 质量验证

- [x] 3.1 执行 `bun run test`、`bun run typecheck`、受影响源文件与新增测试的 Biome lint 及 `bun run build`；验证：全部命令成功且无类型、静态检查或生产构建错误。
